use std::collections::VecDeque;

use nannou::prelude::*;

use crate::model::*;

impl Train {
    /// Returns true if train should be kept
    pub fn update(&mut self, update: &Update, grid_items: &GridItems, trains: &mut VecDeque<Train>) -> bool {
        todo!()
    }
}
